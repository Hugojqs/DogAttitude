article(class="carousel" id="services-slider" data-ride="carousel")
    ol.carousel-indicators
        li(class="active" data-slide-to="0" data-target="#services-slider")
        li(data-slide-to="1" data-target="#services-slider")
        li(data-slide-to="2" data-target="#services-slider")

    div(class="custom-carousel-title bg-white")
        h2.font-weight-bold.text-secondary Le toilettage en 6 étapes

    div(class="carousel-inner" role="listbox")
        include _slide.pug

    a(class="left carousel-control" href="#services-slider" role="button" data-slide="prev")
        span(class="glyphicon glyphicon-chevron-left") <
        span(class="sr-only") Previous

    a(class="right carousel-control" href="#services-slider" role="button" data-slide="next")
        span(class="glyphicon glyphicon-chevron-right") >
        span(class="sr-only") Next
